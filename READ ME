This code is done using Python programming and will work on mostly every offline python compiler (its preferred to use IDLE).
Pls make sure to have the pynput library in the same folder in which this code and the compiler is kept.

Thats it. Now go get them coins ^ ^
